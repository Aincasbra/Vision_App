# Sistema de Detección YOLO + Aravis para Jetson Orin
# Dependencias compatibles con JetPack 5.1.1 (Ubuntu 20.04)
# Python 3.8, CUDA 11.4, PyTorch 1.13.x, Aravis 0.6


#Instalar en el sistema no en el entorno virtual ./venv: 
sudo apt update
sudo apt install -y libopenblas-base libopenblas-dev libomp5

# Core dependencies - Compatibles con JetPack 5.1.1
numpy==1.24.4
torch==1.13.1
torchvision==0.14.1
torchaudio==0.13.1
ultralytics==8.0.196

# Camera and hardware (Aravis 0.6 compatible)
# PyGObject se instala desde el sistema: sudo apt install python3-gi
Jetson.GPIO==2.0.17

# Utilities - Versiones compatibles con Python 3.8
PyYAML==6.0
psutil==5.9.0
pillow==9.5.0
matplotlib==3.6.0
scipy==1.9.0
pandas==1.5.0

# OpenCV - Versión compatible con JetPack 5.1.1
opencv-python-headless==4.6.0.66

# Optional: ONNX Runtime (versión compatible)
onnxruntime==1.12.1

# Development and diagnostics
click==8.1.0
coloredlogs==15.0.1

# Dependencias adicionales para JetPack 5.1.1
setuptools==65.5.0
wheel==0.37.1

# NOTA: Para instalar Aravis 0.6 (compatible con JetPack 5.1.1):
# sudo apt install -y libaravis-dev gir1.2-aravis-0.6 python3-gi python3-gi-cairo gir1.2-gtk-3.0
